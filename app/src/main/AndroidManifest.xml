<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--For Retrofit Future Request-->
    <uses-permission android:name="android.permission.INTERNET" />
    <!--@mipmap/ic_launcher-->

    <application
        android:name=".app_system.AppController"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_app_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_app_logo_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Alqema"
        tools:targetApi="31">

<!--        <activity
            android:name=".ui.screens.CreationActivity"
            android:exported="false" />

        <activity
            android:name=".ui.screens.DisplayActivity"
            android:exported="false" />-->

        <activity
            android:name=".ui.screens.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>